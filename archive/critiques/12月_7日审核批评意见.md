You act as a senior ML engineer.

Goal:
For my existing pruning framework (ResNet-20/56/110 on CIFAR-10/100 and ResNet-18 on Tiny-ImageNet),
create a new script `run_multi_seed.py` that:

1. Accepts:
   --dataset
   --model
   --method {l1, fpgm, hrank, gra}
   --prune_ratio
   --seeds (list of int, e.g. [0,1,2])
2. For each seed:
   - load pretrained baseline
   - apply pruning
   - fine-tune
   - evaluate Top-1 accuracy
3. After running all seeds:
   - compute mean and std of accuracy
   - recompute FLOPs and params once (shared)
   - append a row to `results_multi.csv`:
       dataset, model, method, prune_ratio, mean_acc, std_acc, flops, params
4. Use CUDA, AMP, and tqdm.

Please generate the full Python code for `run_multi_seed.py`.
Write a script `run_resnet110_cifar10.py` that:

- Runs pruning experiments on CIFAR-10 with ResNet-110.
- Methods: L1, GRA.
- Prune ratios: 0.5 and 0.7.
- Uses my existing pruning classes.
- Trains/fine-tunes with:
    epochs = 160 (baseline, if needed)
    finetune_epochs = 60
- Logs results to `results_resnet110.csv`.

At the end, generate a small LaTeX table comparing:
   - Baseline
   - L1@0.5,0.7
   - GRA@0.5,0.7
with columns: Method, Ratio, Top-1 Acc, FLOPs Red (%), Params Red (%).
Create `run_tinyimagenet_resnet18_extended.py` that:

1. Loads Tiny-ImageNet and a pretrained ResNet-18.
2. Runs pruning for:
    methods = [l1, fpgm, gra]
    prune_ratios = [0.3, 0.5]
3. For each (method, ratio):
    - apply structured channel pruning
    - fine-tune ~30 epochs with cosine LR
    - evaluate Top-1 accuracy
    - compute FLOPs, params
    - append to `results_tiny.csv`.

Finally, produce a LaTeX table code block summarizing all results for Table 2.
Write a script `benchmark_latency.py` that:

1. Benchmarks inference latency on a single RTX 5090 GPU using PyTorch.
2. Models to test:
   - resnet20_cifar10_baseline.pth
   - resnet20_cifar10_gra_pruned_0.5.pth
   - resnet56_cifar10_baseline.pth
   - resnet56_cifar10_gra_pruned_0.5.pth
   - resnet18_tiny_baseline.pth
   - resnet18_tiny_gra_pruned_0.5.pth
3. For each model:
   - Use batch_size = 128, resolution = 32x32 or 64x64 accordingly.
   - Warm up 50 iterations (no timing).
   - Measure next 200 iterations with `torch.cuda.Event` or `time.perf_counter`.
   - Compute average latency per batch and per image.
   - Compute throughput (images per second).
4. Save results to `latency_results.csv` with columns:
   model_name, dataset, pruned, flops, latency_ms, images_per_sec.

Then, generate a LaTeX table summarizing latency vs FLOPs reduction.
