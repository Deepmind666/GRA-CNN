\documentclass[authoryear,preprint,review,12pt]{elsarticle}

\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{algorithm}
\usepackage{algorithmic}

\journal{Pattern Recognition Letters}

\begin{document}

\begin{frontmatter}

\title{GRA-CNN: A Novel Convolutional Neural Network Method with Gray Relational Analysis}
\date{December 6, 2025}

\author[1]{Xiaobo Zhang}
\author[1]{Kangrui Li\corref{cor1}}
\ead{1403073295@mails.gdut.edu.cn}
\author[1]{Junyi Lin}
\author[1]{Yuting Wen}

\cortext[cor1]{Corresponding author}
\address[1]{Faculty of Automation, Guangdong University of Technology, Guangzhou, China}

\begin{abstract}
Deep convolutional neural network is one of the most important deep learning models, widely used in image recognition, speech recognition and other fields. In the research and application of convolutional neural networks, the number of hidden layers and nodes is generally determined by experience and expert knowledge. In this sense, achieving optimal network architecture with excellent generalization capabilities and low model complexity is time consuming and even impossible. In order to solve this problem, this paper proposes a gray correlation analysis method to optimize the convolutional neural network (GRA-CNN). The method evaluates the correlation between hidden nodes and output nodes in the network model and selects hidden nodes with higher correlation. Deleting hidden nodes with smaller degrees ensures that the final convolutional neural network can have better recognition accuracy while maintaining higher training performance and higher system stability and adaptability. Experiments on several benchmark datasets show that the proposed method is superior to many other methods in terms of model performance or model complexity.
\end{abstract}

\begin{keyword}
Deep Learning \sep Convolutional Neural Network \sep Gray Relational Analysis \sep Image Recognition
\end{keyword}

\end{frontmatter}

\section{Introduction}
Convolutional neural networks (CNNs) have achieved remarkable success in various computer vision tasks. However, the deployment of deep CNNs on resource-constrained devices is hindered by their high computational cost and storage requirements. Model pruning has emerged as an effective technique to compress and accelerate CNNs by removing redundant parameters.

In this paper, we propose GRA-CNN, a novel channel pruning method based on Gray Relational Analysis (GRA). GRA-CNN evaluates the importance of each filter by calculating the gray relational degree between its feature maps and the final classification logits. This global, semantic-aware metric allows GRA-CNN to identify and preserve filters that are most critical for the network's decision-making process, leading to superior performance compared to traditional magnitude-based or local importance metrics.

\input{vis/related_work_revised.tex}

\section{Methodology}
The core idea of GRA-CNN is to utilize Gray Relational Analysis to measure the correlation between the feature maps of intermediate layers and the network's output logits. We treat the output logits as the reference sequence and the channel-wise activations as comparative sequences. A higher gray relational grade indicates a stronger contribution to the final decision.

Let $X_0 = \{x_0(k) | k = 1, 2, ..., n\}$ be the reference sequence (logits) and $X_i = \{x_i(k) | k = 1, 2, ..., n\}$ be the comparative sequence (channel activation), where $n$ is the number of samples. The Gray Relational Coefficient $\xi_i(k)$ is calculated as:

\begin{equation}
\xi_i(k) = \frac{\min_i \min_k |x_0(k) - x_i(k)| + \rho \max_i \max_k |x_0(k) - x_i(k)|}{|x_0(k) - x_i(k)| + \rho \max_i \max_k |x_0(k) - x_i(k)|}
\end{equation}

where $\rho \in [0, 1]$ is the resolution coefficient, typically set to 0.5. The Gray Relational Grade $r_i$ is the average of the coefficients:

\begin{equation}
r_i = \frac{1}{n} \sum_{k=1}^{n} \xi_i(k)
\end{equation}

Filters with smaller $r_i$ are considered redundant and pruned.

\section{Experiments}
We evaluate GRA-CNN on CIFAR-10, CIFAR-100, and Tiny-ImageNet datasets using ResNet-20, ResNet-56, and ResNet-18 architectures. We compare our method with state-of-the-art pruning techniques including L1-Norm, FPGM, and HRank.

\subsection{Results on CIFAR-10}
We conducted extensive experiments on CIFAR-10 to validate the effectiveness of GRA-CNN. Figure \ref{fig:res20_cifar10} and Figure \ref{fig:res56_cifar10} show the accuracy-pruning ratio curves for ResNet-20 and ResNet-56, respectively. GRA-CNN consistently outperforms the baselines, especially at high pruning ratios.

\begin{figure}[ht]
\centering
\includegraphics[width=0.7\textwidth]{fig2.pdf}
\caption{Performance comparison of ResNet-20 on CIFAR-10. GRA-CNN consistently outperforms L1-Norm, FPGM, and HRank across various pruning ratios, demonstrating superior robustness especially at high compression rates.}
\label{fig:res20_cifar10}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=0.7\textwidth]{fig3.pdf}
\caption{Performance comparison of ResNet-56 on CIFAR-10. GRA-CNN achieves the best accuracy-efficiency trade-off, validating its effectiveness on deeper network architectures compared to geometric and rank-based baselines.}
\label{fig:res56_cifar10}
\end{figure}

\input{vis/table_r56.tex}

\subsection{Results on Tiny-ImageNet}
To demonstrate the scalability of our method, we evaluated GRA-CNN on the Tiny-ImageNet dataset.

\input{vis/table_tiny.tex}

\subsection{Ablation Study}
We analyzed the impact of the resolution coefficient $\rho$ on pruning performance. Figure \ref{fig:rho_ablation} shows that the performance is relatively stable for $\rho \in [0.3, 0.7]$, with the best trade-off achieved at $\rho=0.5$.

\begin{figure}[ht]
\centering
\includegraphics[width=0.7\textwidth]{fig7.pdf}
\caption{Sensitivity analysis of the resolution coefficient $\rho$ for GRA-CNN on ResNet-20. The method shows stable performance across a wide range of $\rho \in [0.3, 0.7]$, with the optimal trade-off observed at $\rho=0.5$.}
\label{fig:rho_ablation}
\end{figure}

Figure \ref{fig:correlation} illustrates the correlation between L1-Norm scores and GRA scores. The low correlation suggests that GRA captures different, likely semantic, information compared to magnitude-based metrics.

\begin{figure}[ht]
\centering
\includegraphics[width=0.7\textwidth]{fig6.pdf}
\caption{Correlation analysis between L1-Norm and GRA scores. The scatter plot reveals a low correlation (Pearson $r=0.12$), indicating that GRA identifies a distinct set of important channels often overlooked by magnitude-based criteria. The quadrant analysis highlights channels with low magnitude but high semantic importance preserved by GRA.}
\label{fig:correlation}
\end{figure}

\input{vis/conclusion_revised.tex}

\bibliographystyle{elsarticle-harv}
\bibliography{manuscript_fixed}

\end{document}
